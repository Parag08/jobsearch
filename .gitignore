node_modules/
.next/
out/
coverage/
*.tsbuildinfo
next-env.d.ts
.env
.env.local
.env*.local
.DS_Store
.vercel

# Agent tooling: installed skills are vendored third-party content, not product code.
# skills-lock.json is kept so the set is reproducible via `npx skills install`.
.agents/
.claude/
